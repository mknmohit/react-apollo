type Query {
  getAllPosts: [Post]
  getSinglePost(id: ID!): Post
}

type Mutation {
  addNewPost(title: String!, description: String!): Post
}

type Post {
  id: ID
  title: String!
  description: String!
}
